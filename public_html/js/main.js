requirejs.config({
    baseUrl: 'js/',
    paths: {
        sections: 'app/sections',
        models: 'app/models',
        helpers: 'app/helpers'
    }
});

require(
[
    'libs/kage'
],
// main application function (starter)
function(
    kage
) {
	
    // TODO: your starter code here
	
});


