requirejs.config({
    shim: {
        jquery: {
            exports: 'jQuery'
        }
    }
});

require(
[ // deps
	'libs/kage',
	'config/application'
],
// main application function (starter)
function(kage) {
	
    // TODO: your starter code here
	
});


